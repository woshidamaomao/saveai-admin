# admin 项目专用（与本目录 docker-compose 一起使用）
# 使用：cp .env.example .env

# 前端构建时写入的 API 根路径（须含 /api）
VITE_API_BASE_URL=https://api-service.saveai.net/api

# 镜像标签
IMAGE_TAG=latest

# 仅 docker-compose.yml 默认模式：宿主机映射端口（叠加 traefik 文件时也可保留作本机调试）
ADMIN_HTTP_PORT=8080
